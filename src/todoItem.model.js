class TodoItemModel {
    constructor(id, itemText, date, status) {
        this.id = id;
        this.itemText = itemText;
        this.date = date;
        this.status = status;
    }
}

export default TodoItemModel;